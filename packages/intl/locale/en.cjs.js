'use strict';

var manager = require('@niche-js/intl');

var messages = {
  "0aV7v8": [{
    type: 0,
    value: "Image label"
  }],
  "OPxJx/": [{
    type: 0,
    value: "Layout"
  }]
};

/* eslint-disable */
manager.addLocale('en', messages);
